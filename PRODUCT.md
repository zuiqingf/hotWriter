# HotWriter · 产品文档

> 个人热点写作助手 · 用户视角 · 2026-08

---

## 目录

- [1. 一句话定位](#1-一句话定位)
- [2. 目标用户](#2-目标用户)
- [3. 核心功能](#3-核心功能)
- [4. 用户流程](#4-用户流程)
- [5. 详细功能说明](#5-详细功能说明)
- [6. 适用场景](#6-适用场景)
- [7. 不适用场景](#7-不适用场景)
- [8. 设计理念](#8-设计理念)
- [9. 未来规划](#9-未来规划)
- [10. 常见问题](#10-常见问题)

---

## 1. 一句话定位

> **把"热点 → 多平台合规文章"的链路打通，让一个人写公众号/小红书/头条/知乎像复制粘贴一样快。**

---

## 2. 目标用户

**唯一目标用户**：个人内容创作者 + 多平台运营者

- **公众号 / 知乎 / 头条 / 小红书博主**：追热点、出内容
- **副业尝试者**：想低成本做内容，不知道从哪下手
- **写作爱好者**：懒得从 0 写，但有自己的观点想分享

**不是**：
- ❌ 商业团队（没有协作、没有权限、没有审批流）
- ❌ 完全无写作能力的人（AI 帮你写 80%，你得能审、改、定）
- ❌ SEO 刷量玩家（不靠关键词堆砌取胜）

---

## 3. 核心功能

### 3.1 功能全景

| 功能 | 一句话价值 | 实现成熟度 |
|------|-----------|-----------|
| **热搜聚合** | 4 平台热点一屏看完，不用挨个刷 | ✅ 完成 |
| **关键词调研** | 输入主题，AI 主动查资料 +给方向 | ✅ 完成 |
| **多平台风格重写** | 同一篇文章，一键转知乎/小红书/头条/公众号风格 | ✅ 完成 |
| **AI 自动写** | 调研完直接生成全文初稿 | ✅ 完成 |
| **AI 改写** | 选段文字，润色/扩写/缩写/改语气/翻译/校对 | ✅ 完成 |
| **富文本编辑器** | 像 Word 一样编辑，支持斜杠菜单、气泡工具栏 | ✅ 完成 |
| **图片上传** | 拖拽 / 粘贴 / 选文件 → 一键插入 | ✅ 完成 |
| **一键校验** | 文章发到哪个平台之前，先过一遍合规检查 | ✅ 完成 |
| **双格式复制** | 粘贴到公众号/小红书保留格式 | ✅ 完成 |
| **MD 下载** | 导出为标准 Markdown，可上传到其他平台 | ✅ 完成 |

### 3.2 核心用户旅程（5 步出文）

```
步骤 1：逛热点
  ↓ 首页看 4 平台热搜，挑一个感兴趣的
步骤 2：调研
  ↓ 点 "一键写" → AI 多轮调研 → 3-5 个差异化方向
步骤 3：选方向 + 自动写
  ↓ 选一个方向 → AI 生成初稿 → 进入工作台
步骤 4：调格式
  ↓ 在左列对话 "改成小红书风格" / "扩写第三段"
  ↓ 在右列编辑器用 AI 改写（润色/扩写/缩写）
步骤 5：合规 + 发布
  ↓ 点 "一键校验" → 选平台 → 看违规项 + 修复建议
  ↓ 点 "下载" / "复制" → 粘贴到目标平台
```

**总耗时**：熟练用户 5-10 分钟从热点到合规初稿。

---

## 4. 用户流程

### 4.1 首页（信息流入口）

**布局**：

```
┌─────────────────────────────────────────┐
│ [搜索框] [开始研究]                  │ ← 顶部
├─────────────────────────────────────────┤
│ 今日热门: 副业做博主... 一诺千金...  │ ← 推荐词
├─────────────────────────────────────────┤
│ 澎湃新闻                                │
│ 1. xxx [✍️ 一键写]              │ ← hover 显示
│ 2. yyy                                │
│ ...                                    │
├─────────────────────────────────────────┤
│ 今日头条                                │
│ ...                                    │
└─────────────────────────────────────────┘
```

**核心交互**：
- 顶部搜索框：手动输入任意关键词
- 推荐词：点击直接填到搜索框
- 一键写（hover 显示）：直达调研页

### 4.2 调研页（自动化产出方向）

**用户操作**：

1. 进入 `/research?keyword=xxx&url=...&auto=1`
2. 看 AI 实时调工具（搜索、知乎、小红书）— 进度条 + step 折叠
3. 3-5 个方向卡：
   - **科普向** / **数据向** / **故事向** / **争议向** / **实操向**（图标区分）
   - 每个方向：标题候选、目标读者、调性、字数、结构化提纲、关键素材
4. 用户选一个方向 → 自动创建文章 + 触发自动写 → 跳转工作台

### 4.3 工作台（核心交互界面）

**布局（编辑器展开）**：

```
┌──────────────────────────────────────┬────────────────────┐
│   ✍️ 编辑器 (60%)        │ ← 返回 │标题 │字数│   │
├──────────────────────────────────────┼────────────────────┤
│   修改于 时间 · 2255 字            │ 💬 对话 (40%)      │
│   [↶ ↷] [公众号 ▼] [✓ 校验]       │                      │
│   [复制] [下载] [···] [@ AI] [×]   │ 消息流...           │
│                                      │                      │
│   富文本内容                          │                      │
│   标题                                │                      │
│   段落                                │                      │
│   ...                                │ [换个风格] [输入框]  │
└──────────────────────────────────────┴────────────────────┘
```

**布局（编辑器隐藏）**：

- 进入时默认隐藏（专注聊天）
- 用户点 "应用到正文" / "AI 改写" → 滑出
- 用户点 `×` 或 "收起" → 滑回

**编辑器头部**（9 个工具）：

```
[修改于 刚刚 · 2255 字]   ↶ ↷ │ [公众号 ▼] [✓ 校验] [复制] [下载] [···] │ @ AI ×
```

| 按钮 | 作用 |
|------|------|
| ↶ ↷ | 撤销 / 重做 |
| [公众号 ▼] | 切换校验目标平台（知乎/小红书/头条/公众号）|
| ✓ 校验 | 一键合规检查 |
| 复制 | 双格式复制（HTML + Markdown），粘贴到公众号保留格式 |
| 下载 | 导出 `.md` 文件（含完整格式）|
| ··· | 更多（清空 / 复制HTML / 复制纯文本）|
| @ AI | 无选区时的 AI 快捷入口 |
| × | 收起编辑器 |

**气泡工具栏**（选中文字时）：

```
[B][I][S][</>][|][🔗][|][✨润色][📝扩写][✂️缩写][⚡ 更多▾]
```

`⚡ 更多` 展开：
- 💼 更正式 / 💬 更口语 / 🎯 更有说服力
- 🌐 英译 / 💡 解释 / 📋 总结 / ✓ 校对

**斜杠菜单**（输入 `/`）：

```
/  → [H1][H2][H3][列表][引用][代码块][分割线][图片][链接]
```

### 4.4 AI 改写：预览模式（关键交互）

**为什么不直接替换？**

**流程**：

```
选中段落 → 点 [✨润色]
   ↓
弹预览窗口（左原文，右 AI 流式生成）
   ↓
对比决定：
├─ ✓ 应用到原文  → 替换选区
├─ 取消          → 选区不动
└─ 🔄 重新生成  → 同一 action 重跑
```

窗口可拖动，方便用户边看 AI 建议边改页面。

### 4.5 一键合规校验

**流程**：

```
点 [✓ 校验]
   ↓
LLM 对照内置的「目标平台规则库」检查
   ↓
弹出可拖动面板：
┌────────────────────────────────────┐
│ [评分] 公众号 合规校验 [×]        │ ← 拖动头部
├────────────────────────────────────┤
│ ⚠ 必须修改的违规 (4)              │
│ ┌────────────────────────────────┐ │
│ │ [高危] 广告法违规            │ │
│ │ 触发: 全网最优的产品        │ │
│ │ 📋 禁用极限词「最」          │ │
│ │ 💡 怎么改：替换绝对化用语  │ │
│ │ ✅ 建议改为：[📋复制]     │ │
│ │ ┌────────────────────────┐ │ │
│ │ │ 这款产品在同类中表现优异 │ │ │
│ │ └────────────────────────┘ │ │
│ └────────────────────────────────┘ │
├────────────────────────────────────┤
│ ⚠ 建议优化 (1)                    │
│ 💡 通用建议                       │
└────────────────────────────────────┘
```

**色彩编码**：
- 80-100 分：绿（✓ 通过）
- 50-79 分：琥珀（⚠ 警告）
- 0-49 分：红（✗ 违规）

---

## 5. 详细功能说明

### 5.1 热搜聚合

**数据源**（4 平台）：
- 今日头条（官方 hot-board API）
- 百度热搜（top.baidu.com HTML）
- 澎湃新闻（官方 contVisit API）
- 抖音总榜（tophub.today 第三方）

**特性**：
- 30 分钟 revalidate
- 任意源失败 → 降级到兜底示例
- 历史趋势自动入库（24h 去重窗口）

### 5.2 关键词调研

**Agent 流程**：
1. LLM 主动决定调哪些工具
2. 多轮调用：通用 web_search / 知乎搜索 / 小红书搜索 / fetch_url
3. 最长 8 轮（防死循环：MAX_ROUNDS + 强制收敛）
4. 输出 3-5 个差异化方向

**每个方向包含**：
- `title` + `title_alt`（主选 + 备选标题）
- `angle`：科普 / 数据 / 故事 / 争议 / 实操
- `target_audience`：目标读者
- `tone`：调性
- `word_count`：字数建议（按 angle 差异化）
- `outline`：结构化提纲
- `key_materials`：关键素材引用
- `rationale`：为什么这是好的写作方向

### 5.3 自动写

**触发**：选中方向后自动跑

**输出**：完整 Markdown 格式的文章（1500 字左右，按 word_count 配置）

**写入**：
- 内容存入 `articles.content`（HTML 格式）
- 同时写入对话流（用户能看到实时生成）

### 5.4 多平台风格重写

**入口**：工作台左下角 [换个风格] 按钮组

**支持平台**：

| 平台 | 字数 | 风格 |
|------|------|------|
| 知乎 | 1500-2500 | 专业深度、有理有据 |
| 小红书 | 800-1500 | 口语 + emoji 排版 |
| 头条 | 1200-1800 | 开头抓眼、有数据 |
| 公众号 | 1500-3000 | 长文、开头 AI 标注 |

**输出**：在对话流里以 AI 消息展示，**不覆盖**原文（用户可手动对比采纳）

### 5.5 富文本编辑器

**基础**：
- 加粗 / 斜体 / 删除线 / 行内代码 / 链接 / 标题 / 列表 / 引用 / 代码块 / 分割线

**Markdown 快捷**：输入 `# ` 转 H1，`**` 转粗体，`> ` 转引用等

**斜杠菜单**：输入 `/` 唤出命令面板（键盘 ↑↓ 选择，Enter 触发）

**气泡工具栏**：选中文字时弹出

**悬浮 + 按钮**：空行时左浮"+"，点击触发 `/`

**图片插入**：
- 斜杠菜单 → 图片（文件选择）
- 拖拽图片到编辑器
- 粘贴图片

**图片上传**：≤ 5MB，自动按 YYYYMM 分目录保存到 `/public/uploads/`

### 5.6 AI 改写

**10 个 action**（按使用频率排序）：

| 类别 | Action | 何时用 |
|------|--------|--------|
| 高频（直接展示）| ✨ 润色 | 写完了想统一调优文笔 |
|  | 📝 扩写 | 字数不够，需要丰富细节 |
|  | ✂️ 缩写 | 写太啰嗦，需要精炼 |
| 次频（更多菜单）| 💼 更正式 | 公文/报告场景 |
|  | 💬 更口语 | 朋友圈/小红书 |
|  | 🎯 更有说服力 | 营销/观点文 |
|  | 🌐 英译 | 出海内容 |
|  | 💡 解释 | 解释专业术语 |
|  | 📋 总结 | 长段落压缩 |
|  | ✓ 校对 | 错别字/语病 |

**预览窗口**：
- 左原文 vs 右 AI 改写
- 流式显示（边生成边出现）
- 用户主动点"应用"才替换
- 可拖动 + 边界限制

### 5.7 一键合规校验

**4 平台规则库**（内置在 prompt 里）：

每个平台 ~800 tokens 规则，包括：
- 通用合规底线（AI 标注、广告法、商业披露、导流、抄袭）
- 平台专属规则（字数、配图、标题、流量分发）
- 严查重点（极限词、医美、违禁内容等）

**输出**：
- 评分 0-100
- 严重违规（必须修改）
- 警告（建议优化）
- 通用建议

**每条违规的解决方案**：
- `fix`：修复后的版本（可直接复制粘贴）
- `suggestion`：简短说明怎么改

### 5.8 多格式复制

**双格式 ClipboardItem**：
- `text/html`：富文本（公众号/小红书编辑器粘贴时识别）
- `text/plain`：Markdown 原文（Notion/Typora 粘贴时识别）

**目标应用自动选用**：

| 粘贴到 | 用哪个 |
|--------|--------|
| 公众号编辑器 | HTML |
| 小红书编辑器 | HTML |
| Notion | Markdown |
| Typora | Markdown |
| 普通微信框 | 纯文本 |

### 5.9 下载 Markdown

**实现**：HTML → turndown → `.md` 文件下载

**保留**：标题、列表、粗体、代码块、链接、图片、引用、分割线

---

## 6. 适用场景

### 6.1 个人内容创作者

**场景**：今天有个热点想跟 → 不想手动搜资料 → 不想从 0 写 → 一小时出稿

**流程**：
```
打开首页 → 看到澎湃热搜有个感兴趣的话题
点 "一键写" → 看 AI 调研的方向
选一个方向 → 等 AI 出初稿
在工作台用 AI 改写调优 → 点一键校验过合规 → 复制粘贴到公众号
```

**预期收益**：1 小时 → 15 分钟

### 6.2 多平台运营

**场景**：同一选题要在公众号、小红书、知乎、头条各发一遍

**流程**：
```
写完一篇公众号长文
在工作台左下角点 "小红书版" → AI 输出小红书版本
复制粘贴到小红书
点 "头条版" → 输出头条版本
复制粘贴
```

### 6.3 副业尝试者

**场景**：想低成本试水自媒体，但不知道写什么 / 怎么写

**流程**：
```
看首页热搜 → 找一个感兴趣的
让 AI 调研给方向 → 选一个自己能聊的
AI 出初稿 → 改成自己的话
合规校验 → 发布
```

---

## 7. 不适用场景

- **团队协作**：没有权限管理 / 多人编辑
- **学术论文**：风格不匹配、需要严格的引用规范
- **法律 / 医疗专业内容**：合规风险极高，不建议完全靠 AI
- **SEO 关键词堆砌**：本工具不鼓励这种做法
- **完全无写作能力**：AI 是助手不是替代品

---

## 8. 设计理念

### 8.1 极简优先

- 不做用户系统（个人用，单人即可）
- 不做团队协作（个人用）
- 不做花哨 UI（豆包式极简，灰白为主）
- 专注"内容生成 + 编辑"的核心链路

### 8.2 用户主动确认

- AI 改写**不直接覆盖**（预览窗口让用户决定）
- 下载 / 复制**双格式**（用户粘贴到哪都好用）
- 一键校验**给出 fix**（用户自己决定是否采纳）

### 8.3 反幻觉

- 短关键词不直接给方向（要求带 sourceUrl 先 fetch 原文）
- LLM MAX_ROUNDS 兜底（强制收敛）
- JSON 解析多层 fallback（代码块 → 正则提取）

### 8.4 中文优先

- Prompt 全部中文
- UI 文案中文
- 字数统计考虑中文字符 + 英文单词
- 平台规则按中国平台定制（不像海外工具只支持 Medium/Substack）

---

## 9. 未来规划

### 9.1 短期（1-2 周）

- [ ] 模板系统：保存常用写作模板（爆款结构）
- [ ] 一键替换：校验 fix 直接替换原文（不需手动复制）
- [ ] 移动端优化：折叠式布局，tab 切换

### 9.2 中期（1-2 月）

- [ ] 多文章系列（`series_id` 已预留）
- [ ] 数据导出：文章 + 图片 打包 ZIP
- [ ] 校验 fix 批量应用（一键全部修复）
- [ ] 写作历史对比（同一选题 3 次写作对比）

### 9.3 长期

- [ ] 多用户系统（加 auth + 数据库迁移到 Postgres）
- [ ] 实时协作（OT / CRDT）
- [ ] 选题灵感助手（基于历史 + 趋势预测下一个爆款）
- [ ] AI 评注（自动标注"这里可以引用数据""这里加图"）

---

## 10. 常见问题

### Q1：AI 生成的内容会不会被判违规？

A：会的**如果不修改直接发**。本工具的"一键校验"就是用来找出违规项并提供修复方案的。但**最终责任在用户**——AI 不替你担责。

### Q2：AI 调研的方向不满意怎么办？

A：可以：
- 手动输入更具体的关键词
- 选好方向后用对话让 AI 调整方向
- 重新调研（点"重新调研"按钮）

### Q3：编辑器突然打不开 / 卡在加载中？

A：通常是因为 dev server 编译错误导致 `.next/cache` 损坏。修代码后清缓存重启：

```bash
rm -rf .next/cache
npm run dev
```

### Q4：图片上传失败？

A：检查：
- 文件 ≤ 5 MB
- 格式：PNG / JPEG / GIF / WebP / SVG
- 浏览器控制台看具体错误

### Q5：怎么导出文章？

A：编辑器头部 `⬇ 下载` 按钮 → 导出 `.md` 文件（含完整格式）。

### Q6：复制粘贴到公众号没有格式？

A：使用编辑器头部的 `📋 复制` 按钮（不是浏览器原生 Ctrl+C），会保留 HTML 格式。如果还是不行，检查浏览器是否允许剪贴板写入。

### Q7：怎么切换校验平台？

A：编辑器头部 `[公众号 ▼]` → 选 4 平台之一 → 重新点 `✓ 校验`。

### Q8：AI 改写不满意，能重新生成吗？

A：在预览窗口底部点 `🔄 重新生成` 按钮，会用同一 action 重跑（不同结果）。

### Q9：4 平台规则库更新了怎么办？

A：目前需要改代码（[src/app/api/articles/[id]/check-compliance/route.ts](src/app/api/articles/[id]/check-compliance/route.ts)），重新部署。后续可以做"规则库后台管理"。

### Q10：能多人一起用吗？

A：当前是个人单用户版本，没有用户系统。如果要多人用：
1. 加 auth（NextAuth.js 等）
2. 数据库迁移到 Postgres
3. 加 articles.user_id 字段过滤

预计 1-2 周工作量。

---

## 附录：快捷键

| 快捷键 | 作用 |
|--------|------|
| `/` | 唤出斜杠命令菜单 |
| `⌘Z` / `Ctrl+Z` | 撤销 |
| `⌘⇧Z` / `Ctrl+Shift+Z` | 重做 |
| `⌘B` / `Ctrl+B` | 加粗 |
| `⌘I` / `Ctrl+I` | 斜体 |
| `⌘Enter` / `Ctrl+Enter` | 发送对话消息 |
| `↑↓` (斜杠菜单中) | 选择命令 |
| `Enter` (斜杠菜单中) | 触发命令 |
| `Esc` | 关闭弹窗 / 取消选择 |

---

## 附录：术语表

| 术语 | 解释 |
|------|------|
| **方向** | 调研产出的写作角度（如"科普向"、"争议向"）|
| **风格** | 平台调性（知乎/小红书/头条/公众号）|
| **改写** | AI 对选中文字做转换（润色/扩写/缩写/...）|
| **预览模式** | AI 改写不直接替换，先弹窗让用户对比确认 |
| **合规校验** | 检查文章是否符合目标平台规则 |
| **斜杠菜单** | 编辑器里输入 `/` 唤出的命令面板 |
| **气泡工具栏** | 选中文字时弹出的快捷操作栏 |
| **悬浮菜单** | 空行时左浮的 "+" 按钮 |